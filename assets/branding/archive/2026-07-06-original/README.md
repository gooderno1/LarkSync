# LarkSync 原始 Logo 资产归档

归档日期：2026-07-06

本目录保存 v0.8.0 Logo 重做前的原始资产副本。归档只用于回溯和对比，不应被构建脚本直接引用。

目录说明：

- `branding/`：原 `assets/branding/` 中的主 Logo PNG 与 Windows ICO。
- `tray-icons/`：原 `apps/tray/icons/` 中的托盘四态图标。
- `frontend-public/`：原 `apps/frontend/public/` 中的前端 Logo 与 favicon。
